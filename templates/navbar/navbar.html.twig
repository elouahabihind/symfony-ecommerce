<nav>
    <ul>
         <a href="{{ path('home') }}">home</a>
        <a href="{{ path('product_add') }}">Add product</a>
        <a href="{{ path('category_add') }}">Add category</a>
        <a href="{{ path('category') }}">Display categories</a>
    </ul>
</nav>
